(library
 (name mylib)
 (libraries gmp)
 (modules (:standard \ eqRewriter)))

(ocamllex
(modules polyLexer expLexer))

(menhir
(modules polyParse expParse))

(env
 (dev
   (flags (:standard -warn-error -A))))