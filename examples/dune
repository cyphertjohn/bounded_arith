(executables
  (names elastic fixedPointInt manualPrice nirn tokent)
  (libraries bound)
  (promote (until-clean) (into ..))
  ;(flags -cclib -lgmp_caml)
  (modules elastic fixedPointInt manualPrice nirn tokent symBoundBenchmark)
)