(executables
  (names elastic fixedPointInt manualPrice manualPriceMonotone nirn tokent)
  (libraries bound)
  (promote (until-clean) (into ..))
  ;(flags -cclib -lgmp_caml)
  (modules elastic fixedPointInt manualPrice manualPriceMonotone nirn tokent symBoundBenchmark)
)